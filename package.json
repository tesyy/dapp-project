{
  "name": "defi-lending-platform",
  "version": "1.0.0",
  "description": "A Decentralized Lending Platform built with Hardhat and React",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "start": "react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build:cache-bust": "cross-env GENERATE_SOURCEMAP=true PUBLIC_URL=https://tesyy.github.io/dapp-project react-scripts build"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "hardhat": "^2.24.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0",
    "ether": "^0.0.9",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "homepage": "https://tesyy.github.io/dapp-project",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}